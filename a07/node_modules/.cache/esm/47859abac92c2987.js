let keypress,Game;_c92‍.w("keypress",[["default",["keypress"],function(v){keypress=v}]]);_c92‍.w("./engine/game",[["default",["Game"],function(v){Game=v}]]);


keypress(process.stdin);


/**
 * The code in this file is used to run your game in the console. Use it
 * to help develop your game engine.
 *
 */

let game = new Game(4);
//console.log(game.toString());

game.loadGame({
    board: [1024, 1024, 0,2,4,8,2,4,8,8,2,4,4,8,8,16],
    score:0,
    won:false,
    over:false
});
game.move('left');
_c92‍.g.console.log(game.getGameState());







game.onMove(gameState => {
    _c92‍.g.console.log(game.toString());
    //console.log(game.gameState);
});

game.onWin(gameState => {
    _c92‍.g.console.log('You won with a gameState of...', gameState)
});

game.onLose(gameState => {
    _c92‍.g.console.log('You lost! :(', gameState)
    console.log(`Your score was ${gameState.score}`);
});

process.stdin.on('keypress', function (ch, key) {
    switch (key.name) {
        case 'right':
            game.move('right');
            break;
        case 'left':
            game.move('left');

            break;
        case 'down':
            game.move('down');

            break;
        case 'up':
            game.move('up');
            break;
    }
    if (key && key.ctrl && key.name == 'c') {
        process.stdin.pause();
    }
});


process.stdin.setRawMode(true);
process.stdin.resume();

